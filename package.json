{
  "name": "tv-receiver-app",
  "version": "1.0.0",
  "description": "电视投屏接收器 - Android TV APK版本",
  "type": "module",
  "keywords": [
    "tv",
    "cast",
    "receiver",
    "android",
    "dlna",
    "投屏"
  ],
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync android",
    "android:build": "npx cap build android",
    "android:run": "npx cap run android",
    "android:open": "npx cap open android",
    "install:android": "npm run android:add && npm run android:sync",
    "build:apk": "npm run build && npm run android:sync && npm run android:build"
  },
  "dependencies": {
    "@capacitor/core": "^4.8.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^4.8.0",
    "@capacitor/android": "^4.8.0",
    "vite": "^5.4.2"
  },
  "author": "TV Receiver Team",
  "license": "MIT"
}
